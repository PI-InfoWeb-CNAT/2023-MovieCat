@model AgendMovies.Modelos.Funcionario

@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="en">
<head>
    <link rel="stylesheet" href="~/styles/LoginFunc.css">
    <title>HASH TECHIE OFFICIAL</title>
</head>
<body>
    <section>
        <div class="form-box">
            <div class="form-value">
                @using (Html.BeginForm("Login", "Employees", FormMethod.Post,
                        new { enctype = "multipart/form-data" }))

                {
                    <h2>Login</h2>
                    <div class="inputbox">
                        <ion-icon name="mail-outline"></ion-icon>
       
                        @Html.TextBoxFor(model => model.email, new { type = "email", required = "required" })
                        <label for="">Email</label>
                    </div>
                    <div class="inputbox">
                        <ion-icon name="lock-closed-outline"></ion-icon>
                        @Html.TextBoxFor(model => model.senha, new { type = "password", required = "required" })
          
                        <label for="">Senha</label>
                    </div>
                    <div class="forget">
                        <label for=""><input type="checkbox">Lembre de mim    <a href="#">Esqueci minha senha</a></label>

                    </div>
                    <button>Log in</button>
                    <div class="register">
                        <p> <a href="@Url.Action("Index", "Home")">Voltar</a></p>
                    </div>
                }
                </div>
        </div>
    </section>
    <script type="module" src="https://unpkg.com/ionicons@5.5.2/dist/ionicons/ionicons.esm.js"></script>
    <script nomodule src="https://unpkg.com/ionicons@5.5.2/dist/ionicons/ionicons.js"></script>
</body>
</html>